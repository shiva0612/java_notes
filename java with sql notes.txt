java with sql

1) import java.sql.*;
2) form maven repo get the mysql connector and add it to pom.xml
3)connection con = null

4)
String url = "jdbc:mysql://localhost:3306/database_name"
5)
String username = "root",password = "0612"

6)
Class.forName("com.mysql.jdbc.Driver")
con = DriverManager.getConnection(url,username,password)

put this in try and catch if any unhandled exception is showing up

7) // for getting the result query

String sql = "sql_query"

Statement st = con.createStatement()

ResultSet rs = st.executeQuery(sql)

while(rs.next())
{

}

8) // for inserting into the database

String sql = "insert into alien values (?,?,?)"

PreparedStatement st = con.preparedStatement(sql)

// 1,2,3 are the question mark index (1 indicates what to be replaced in 1st question mark)

//we have to know what is the datatype of the coloumn

st.setInt(1,value1) 
st.setString(2,value2)
st.setInt(3,value3) 

st.executeUpdate()
 